#!/bin/bash

set -e

case "$(uname -s)" in
 "Darwin") USER="root:root" ;;
 *) USER="$(id -u):$(id -g)" ;;
esac

docker run --rm --user "$USER" --volume "$PWD:/usr/src/app" --workdir "/usr/src/app" codeclimate/codeclimate-eslint pnpm "$@"
